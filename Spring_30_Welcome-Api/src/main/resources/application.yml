
spring:
  application:
    name: WELCOME-API
  boot:
    admin:
      client:
        url: http://localhost:9090
  cloud:
    compatibility-verifier:
      enabled: false
management:
  endpoints:
    web:
      exposure:
        include: '*'